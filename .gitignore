# Source repos are cloned here by `mega repos sync`, never committed.
/repos/

# Local, machine-specific overrides (provider/model/sandbox name, fork owner).
/mega.local.toml
.env
*.env
!example.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Regenerated by `mega provision` on every run; the rules it documents live in
# README.md, and the file itself is uploaded into the sandbox for the agent.
/TRIANGLE.md

# Tool state
/.charter/.cache/
/.graphify/
/.metaswarm/state/
/state/

# Editors / OS
.DS_Store
*.swp
.idea/
